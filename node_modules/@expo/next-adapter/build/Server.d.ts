/// <reference types="node" />
import { IncomingMessage, Server, ServerResponse } from 'http';
export declare function handleRequest({ projectRoot, app, handle }: {
    projectRoot: string;
    app: NextApp;
    handle: Function;
}, req: IncomingMessage, res: ServerResponse): void;
declare type NextApp = any;
declare type ServerOptions = {
    app: NextApp;
    handle: Function;
    server: Server;
};
export declare function createServerAsync(projectRoot: string, { app: possibleApp, handleRequest: possiblyHandleRequest, }: {
    app?: NextApp;
    handleRequest?: (req: IncomingMessage, res: ServerResponse) => Promise<void> | void;
}): Promise<ServerOptions>;
export declare function startServerAsync(projectRoot: string, { port }?: {
    port?: number;
}): Promise<ServerOptions>;
export {};
